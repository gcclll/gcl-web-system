// Emacs 模板样式

const config: Record<string, string> = {
  // background: 'bg-gradient-to-r from-pink-500 to-fuchsia-800',
  background: 'bg-gradient-to-r from-pink-500 via-red-500 to-yellow-500',
}

export default config
